# CoinCrow pre ICO

Run the app:

```sh
$ npm run testrpc
$ npm run migrate
$ npm start
```

Navigate your browser to [http://localhost:8080](http://localhost:8080/).

Test the contract:

```sh
$ npm run testrpc
$ npm run test
```
